## Interview prep (Python) — consolidated by pattern

All problems in this repo are organized as:

```
<pattern_type>/pXXXX_problem_name.py
```

Examples:

- `array_string/p0238_product_of_array_except_self.py`
- `backtracking/p0131_palindrome_partitioning.py`
- `binary_search/p0074_search_a_2d_matrix.py`

### How to run

Run any file directly; it will execute its built-in tests and then **exit**:

```bash
python array_string/p0238_product_of_array_except_self.py
```

### Study plan

- **14-day plan (senior+ optimized)**: `STUDY_PLAN.md`

### Legacy folders

The folders `leetcode75/`, `top_interview_150/`, and `senior_patterns/` are kept only
for legacy structure (mostly `__init__.py` and helpers). The actual solutions live
in the root-level pattern folders.
